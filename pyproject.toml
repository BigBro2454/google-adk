[project]
name = "google-adk"
version = "0.1.0"
description = "Production multi-agent systems and MCP integration built on Google's Agent Development Kit (ADK) and Gemini 2.5 Flash with resilient local fallbacks."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk>=2.5.0",
    "google-genai>=0.1.0",
    "python-dotenv>=1.0.0",
    "litellm>=1.0.0",
    "requests>=2.31.0",
    "pydantic>=2.0.0",
]
